<resources>
    <style name="Theme.AppCompat.SplashScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Splash screen background color -->
        <item name="android:windowSplashScreenBackground">@color/proactive_red</item>

        <!-- Splash screen animated icon -->
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_proactive_launcher</item>

        <!-- Splash screen animation duration -->
        <item name="android:windowSplashScreenAnimationDuration">4000</item>
    </style>
</resources>